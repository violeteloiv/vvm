# Testing Label Resolution
    jmp 1
    nop
    nop
    nop
# First Two Fibonacci Numbers
    push 0
    push 1
loop: 
    rdup 1 # these two lines are responsible for
    rdup 1 # duplicating the previous two numbers to be added.
    addi
    jmp loop